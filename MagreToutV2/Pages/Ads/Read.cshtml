@page
@model MalgreToutV2.Pages.Ads.ReadModel
@{
    ViewData["Title"] = "GetMagazines";
}
<h2>List of Magazines</h2>
<br />
<a class="btn btn-outline-primary" asp-page="/Magazine/Create">Create</a>
<br />
<br />

<table class="table table-bordered table-responsive-sm table-hover">
    <thead style="background:lightgray">
        <tr>
            <th>Pris</th>
            <th>Firma</th>
            <th></th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>

        @if (Model.ads != null)
        {
            @foreach (var point in Model.ads)
            {
                <tr>
                    <td>@point.AdPrice</td>
                    <td>@point.Company</td>
                    <td><a class="btn btn-outline-info" asp-page="/Ads/Read">View Ads</a> </td>
                    <td><a class="btn btn-outline-secondary" asp-route-id="@point.AdId" asp-page="/Ads/Update">Update</a> </td>
                    <td> <a class="btn btn-outline-danger" asp-page="/Magazine/Delete" asp-route-PickupPointId="@point.AdId">Delete</a></td>
                    @*<td> <a class="btn btn-outline-secondary" asp-page="/PickupPoint/PickupPointInfo" asp-route-Id="@point.PickupPointId">Info</a></td>
                    *@
                </tr>
            }
        }
    </tbody>
</table>