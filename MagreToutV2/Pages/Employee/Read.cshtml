@page
@model MalgreToutV2.Pages.Employee.ReadModel
@{
    ViewData["Title"] = "";
}
<h2><i class="fas fa-users"></i> Liste af medarbejdere</h2>
<a asp-page="/FrontPage">Tilbage til Dashboard</a>
<br />
<br />
<form>
    <p>
        Søg: <input type="text" asp-for="FilterCriteria" />
        <input type="submit" value="Filter" />
        <a asp-page="/Employee/Read">Tilbage til den fulde liste</a>
    </p>
</form>
<a class="btn btn-outline-primary" asp-page="/Employee/Create">Opret</a>
<br />
<br />
<table class="table table-bordered table-responsive-sm table-hover">
    <thead style="background: lightgray;">
        <tr>
            <th>Id</th>
            <th>Navn</th>
            <th>Rolle</th>
            <th>Brugernavn</th>
            <th>Adgangskode</th>
            <th></th>
            <th></th>
            <th></th>

        </tr>
    </thead>
    <tbody>


        @if (Model.Employees != null)
        {
            @foreach (var employee in Model.Employees)
            {
                <tr>
                    <td>@employee.EmployeeId</td>
                    <td>@employee.Name</td>
                    <td>@employee.Role</td>
                    <td>@employee.Username</td>
                    <td>@employee.Password</td>

                    <td>
                        <a class="btn btn-outline-secondary" asp-page="/Employee/employeeInfo" asp-route-id="@employee.EmployeeId">Info</a>
                    </td>
                    <td>
                        <a class="btn btn-outline-danger" asp-page="/Employee/Delete" asp-route-EmployeeId="@employee.EmployeeId">Slet</a>
                    </td>
                    <td>
                        <a class="btn btn-outline-secondary" asp-page="/Employee/Update" asp-route-EmployeeId="@employee.EmployeeId">Opdater</a>

                    </td>


                </tr>

            }}
    </tbody>
</table>